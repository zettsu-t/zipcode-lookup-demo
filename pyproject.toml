[tool.pytest.ini_options]
testpaths = ["backend/tests", "frontend/tests"]
asyncio_mode = "strict"

[tool.ruff]
line-length = 100

[tool.bandit]
# テストコードの assert 文を誤検知しないよう除外する
exclude_dirs = ["backend/tests", "frontend/tests", "tests"]
