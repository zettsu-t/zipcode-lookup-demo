services:
  backend:
    build: ./backend
    ports:
      - "8001:8001"
    volumes:
      - ./backend/data:/app/data:ro
    environment:
      DATA_DIR: data

  frontend:
    build: ./frontend
    ports:
      - "8000:8000"
    environment:
      BACKEND_URL: http://backend:8001
      SECRET_KEY: docker-secret-key  # ⚠ デモ・ローカル専用。本番環境では必ず強力なランダム文字列に変更すること。
      DEBUG: "false"
    depends_on:
      - backend
